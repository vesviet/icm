{
  "name": "@icm/web",
  "version": "1.0.0",
  "description": "icm — ICM Factory Direct main site on Cloudflare",
  "type": "module",
  "private": true,
  "engines": {
    "node": "^18.17.1 || ^20.3.0 || >= 21.0.0"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler pages dev dist",
    "astro": "astro",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.0.0",
    "@astrojs/sitemap": "^3.4.2",
    "@fontsource-variable/inter": "^5.2.6",
    "@fontsource/poppins": "^5.2.7",
    "astro": "^5.12.9",
    "cheerio": "^1.2.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^5.1.5",
    "@tailwindcss/typography": "^0.5.16",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.8.3",
    "wrangler": "^4.0.0"
  }
}
